from dataclasses import dataclass

"""
1. Generating Templates. Each benchmarking class will have a function that when passed a config will write these templates .cu file, then write the corresponding
launchers as need. Faster than writing macros. Can find the files by reading the contents of setup.py.
"""

"""
2. Calls Generate entrypoint functions from benchmarking class imported which dynamically determines which kernel to launcher to call based on config.
"""

"""
3. Calls Generate bindings which uses a special naming system based on the config to create the bindings between entrypoint functions and creates the bindings.
"""

"""
4. Calls compile setup.py to generate the .so files and waits for completion.
"""

"""
5. Runs the benchmarks. Writes the best ones to kernel_configs.json which {device: {kernel: {[situation]: {config}}}}
"""

"""
6. Call Clean up from that class to go in and remove the added templates. Any built kernels will only use the optimal instantiation from a separate build file script.
"""

"""
Write if __main__ to run benchmarks for this gpu. Configs will be generated by the script, not the user.
Import from nearby files.
"""